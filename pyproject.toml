[project]
name = "ai-moderation-content"
version = "0.1.0"
description = "Platform-independent AI moderation core"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
